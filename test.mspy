unit.bind(@quasar)
c = @unit.controller
if (c != @this){if (c != @unit)
{
    utils.end()
}}
x, y, f, b = unit.locateDamaged()
if (f)
{
    n = unit.within(x, y, 10)
    n = n != 1
    unit.boost(n)
    unit.move(x, y)
    unit.target(x, y, 1)
}
else
{
    unit.idle()
}
utils.end()